name: ubuntu-apache-webdav-docker
services:
  ubuntu-apache-webdav-docker:
    ports:
      - 82:80
    environment:
      - WEBDAV_USERNAME=orchid
      - WEBDAV_PASSWORD=orchid
    volumes:
      - /media:/media
    image: ghcr.io/chosomeister/ubuntu-apache-webdav-docker:latest
networks: {}
