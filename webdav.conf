<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName localhost

    Alias /media /media
    <Directory /media>
        Options Indexes FollowSymLinks
        AllowOverride None
        Require all granted
        DAV On
        AuthType Digest
        AuthName "media"
        AuthUserFile /etc/apache2/webdav.password
        Require valid-user
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>

# Set the ServerName globally to suppress the error
ServerName localhost
